#include <stdio.h>

int main(){
	int aluna, aluno; 
	scanf("%d", &aluno);
	scanf("%d", &aluna);
	printf("A sala possui %d aluno(s)", aluno);
	printf("\nA sala possui %d aluna(s)", aluna);
	return 0; 
}
